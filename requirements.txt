# Core Flask and web framework dependencies
Flask==2.3.3
Flask-CORS==4.0.0

# Data processing and analysis
pandas==2.1.1
numpy==1.24.3

# Database
sqlite3  # Built-in with Python, no pip install needed

# Data validation and fuzzy matching
fuzzywuzzy[speed]==0.18.0
python-Levenshtein==0.21.1

# Phone number validation
phonenumbers==8.13.19

# Progress bars for long operations
tqdm==4.66.1

# HTTP requests for API calls
requests==2.31.0

# Date and time handling
python-dateutil==2.8.2

# Regular expressions (built-in, but good to document)
# re - built-in with Python

# Development and testing (optional)
pytest==7.4.2
pytest-flask==1.2.0

# Note: Ollama is installed separately as a system application
# Note: React/Node.js dependencies are in frontend/package.json
